# Example Managed Folder Agent Guide

This is a demo managed folder.

Start with `managed-folder.config.md`, then route current operating work to `_executive/`.

Use `_scribe/` for evidence, logs, and historical notes.

Do not treat this folder as real company data.

# Executive Agent Guide

Read `executive.config.md`.

Use:

- `current_state/` for what is true now.
- `task_radar/` for candidate work that needs review.
- `project_manager/` for approved work.

Keep documents and deliverables outside `_executive/`.
